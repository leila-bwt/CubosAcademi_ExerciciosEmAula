const saldo = 10;

const unidade = (saldo === 1 ? "real" : "reais");

console.log(`Você tem ${saldo} ${unidade}`);

// condiçao é sempre uma expressao boolean
// expr1 e expr2 podem conter valores de qualquer tipo
// expr1 === true, expr2 === false