// inverte a ordem dos itens de um array
// ultimos itens serão os primeiros

const opcoes = ["Eu", "Você", "Ele"];
const resultado = opcoes.reverse();
console.log(resultado);

// esse método muda o array original o tornando invertido
// pois são duas variaves que apontam pro mesmo lugar de memória
console.log(opcoes);